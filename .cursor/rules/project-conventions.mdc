---
description: Стек, структура и общие соглашения проекта Floor Haven
alwaysApply: true
---

# Floor Haven — соглашения проекта

**⚠️ ОБЯЗАТЕЛЬНО:** Каждое сообщение ассистента в этом проекте начинать со смайлика (👍 📋 ✨ 🔧 и т.д.). Перед отправкой ответа проверить: первое слово — смайлик. Пример: «👍 На сайте сейчас такие страницы…»

## Стек
- **Сборка:** Vite 5, TypeScript
- **UI:** React 18, React Router DOM 6
- **Стили:** Tailwind CSS, шрифт Inter
- **Компоненты:** shadcn/ui (Radix), иконки Lucide React

## Структура
- `src/pages/` — страницы-роуты (Index, Catalog, FAQ, Contacts, AboutCompany, NotFound)
- `src/components/` — переиспользуемые компоненты; `components/ui/` — shadcn, не менять без необходимости
- `src/data/` — типы и статические данные (например, products.ts)
- `src/lib/` — утилиты (cn и др.)
- Импорты: алиас `@/` → `src/` (например, `@/components/Header`)

## Язык и контент
- Пользовательский интерфейс и контент — **на русском** (кнопки, заголовки, сообщения, placeholder’ы).
- Комментарии в коде и имена переменных/функций — на английском.

## Стили
- Использовать классы из `src/index.css`: `btn-primary`, `btn-outline`, `card-elevated`, `container-custom`, `section-padding`, `heading-lg`, `text-body`, `input-field` и т.д.
- Новые глобальные классы добавлять в `index.css` в соответствующий `@layer` (components/utilities).

## Общение с пользователем
- Каждое сообщение ассистента в этом проекте начинать со смайлика (например 👍 📋 ✨ 🔧).
